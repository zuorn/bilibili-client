{
  "name": "bilibili-client",
  "version": "0.2.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "build:frontend": "node scripts/build-frontend.js",
    "tauri": "tauri",
    "sync:version": "node scripts/sync-version.js",
    "tauri:dev": "node scripts/tauri-dev.js",
    "tauri:build": "node scripts/tauri-build.js",
    "tauri:build:nsis": "node scripts/tauri-build.js --bundles nsis",
    "tauri:build:mac": "node scripts/tauri-build.js --bundles app,dmg",
    "tauri:build:linux": "node scripts/tauri-build.js --bundles appimage",
    "publish:oss:tauri": "node scripts/publish-oss-tauri.js",
    "publish:oss:tauri:dry": "node scripts/publish-oss-tauri.js --dry-run",
    "publish:github:tauri": "node scripts/publish-github-tauri.js",
    "publish:github:tauri:dry": "node scripts/publish-github-tauri.js --dry-run"
  },
  "keywords": [],
  "author": "Zuorn",
  "license": "ISC",
  "description": "Bilibili client",
  "dependencies": {
    "ali-oss": "^6.23.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.5",
    "esbuild": "^0.28.2",
    "js-yaml": "^4.1.1",
    "rimraf": "^6.1.3",
    "sharp": "^0.34.5"
  }
}
